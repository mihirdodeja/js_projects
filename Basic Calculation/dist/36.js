"use strict";
// var time=4 // 1.5 HOURS
var speed = 100; //100 KMS/HOUR
var distance = 20; //20 KMS
var time = distance / speed;
console.log(`Time using Distance ${distance} / speed ${speed} captured is: `, time);
